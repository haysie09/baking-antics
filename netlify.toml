[build]
  command = "npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions."fetch-recipe"]
  timeout = 20
  memory = 1024
  node_bundler = "esbuild"
  node_version = "20"